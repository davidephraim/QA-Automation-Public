# New Feature


---

# Stable Feature
This script can generate all personal information of user. Can be use on dev or staging.

```bash
pytest set_personal_info.py --env= --username=<USER_EMAIL> --password=<USER_PASSWD> --name=<USER_NAME> --headed
```